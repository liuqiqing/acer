<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.inspur.dao.syck.SyckMapper">
    <!-- 测试 -->
    <select id="getSum" resultType="java.lang.Integer">
        SELECT  COUNT (1) AS  SUM FROM ACCEPT_BUSINESS_INDEX
    </select>
    <!--省收件数-->
    <select id="ssjs" resultType="int">
        select count(1) from ACCEPT_BUSINESS_INDEX
        WHERE TO_CHAR(SUBMIT_TIME,'yyyy-mm-dd') = #{Cdate} and region_code='370000000000'
    </select>
    <!--省收件时间顺序排行-->
    <select id="ssjph" resultType="java.util.LinkedHashMap">
       select item_name,applicant,submit_time,region_name,type from ACCEPT_BUSINESS_INDEX
        WHERE TO_CHAR(SUBMIT_TIME,'yyyy-mm-dd') = #{Cdate}
        order by submit_time desc
    </select>
</mapper>